{
  "name": "alquileres-la-casona-functions",
  "private": true,
  "main": "lib/index.js",
  "engines": {
    "node": "20"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "clean": "powershell -Command \"if (Test-Path lib) { Remove-Item -Recurse -Force lib }\"",
    "lint": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@types/pdfkit": "^0.17.6",
    "firebase-admin": "^12.7.0",
    "firebase-functions": "^6.0.1",
    "jszip": "^3.10.1",
    "nodemailer": "^8.0.7",
    "pdfkit": "^0.18.0",
    "qrcode": "^1.5.4",
    "svg-to-pdfkit": "^0.1.8"
  },
  "devDependencies": {
    "@types/nodemailer": "^8.0.0",
    "typescript": "^5.6.3"
  }
}
